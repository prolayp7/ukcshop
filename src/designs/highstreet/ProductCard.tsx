// Same card everywhere: the category grid's card is the one product-card
// design the site has, so every rail/grid just renders it directly instead
// of maintaining a second, visually-diverging copy.
export { default } from "@/components/pages/CategoryProductCard";
